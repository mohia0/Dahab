name: Debug live prices now v2
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: gold-api.com right now
        run: curl -sS -m 10 "https://api.gold-api.com/price/XAU"
      - name: er-api fx right now
        run: curl -sS -m 10 "https://open.er-api.com/v6/latest/USD" | head -c 200
